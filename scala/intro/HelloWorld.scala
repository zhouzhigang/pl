/**
 * The first Scala program: print "Hello, World!".
 *
 * Compile:
 *  scala HelloWorld.scala
 * Compile with target directory
 *  scalac -d classes HelloWorld.scala
 *
 * Run:
 *  scala HelloWorld
 * Run with classpath
 *  scala -cp classes HelloWorld
 */
object HelloWorld {
    def main(args: Array[String]) {
        println("Hello, World!")
    }
}
